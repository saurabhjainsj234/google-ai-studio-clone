export type UserProfile = {
  id: string;
  name: string;
  email: string;
  avatarUrl: string;
};

export type DashboardCard = {
  title: string;
  description: string;
  value: number;
};

export type Feature = {
  title: string;
  description: string;
  icon: string;
};